{
  "name": "fs-open-backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../fs-open/part3/phonebook && npm run build --prod && cp -r build ../../../fs-open-backend",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mikokm/fs-open-backend.git"
  },
  "author": "Miko Mynttinen",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mikokm/fs-open-backend/issues"
  },
  "homepage": "https://github.com/mikokm/fs-open-backend#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.11.16",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
